export const OTP_EXPIRATION_DURATION = 10 * 60 * 1000; // 5 minutes

export const AT_VALIDITY_DURATION = 15 * 60 * 1000; // 15 minutes

export const RT_VALIDITY_DURATION = 20 * 60 * 1000; // 15 minutes

export enum AttendeeRoles {
  ATTENDEE = "ATTENDEE",
  HOST = "HOST",
  MODERATOR = "MODERATOR",
}

export enum MediaType {
  PHOTO = "PHOTO",
  VIDEO = "VIDEO",
}
